name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: 'Checkout codes'
      uses: actions/checkout@v1
    - name: build
      run: |
          sh ${{ github.workspace }}/build.sh
    - name: build_faster
      run: |
          sh ${{ github.workspace }}/build_faster.sh
